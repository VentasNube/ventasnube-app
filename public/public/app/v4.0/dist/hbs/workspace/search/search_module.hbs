<!--searchBox -->
<div id="searchBox" class="search-content ">
    <!--search-header -->
    <div class="search-header">
        <div class="search-nav">
            <button type="button" class="search_close">
                <span class="material-icons">arrow_back</span>
            </button>
            <div class="group">
                <!--span class="badge round xs">Productos <button>
                        <span class="material-icons">cancel</span></button>
                </span-->
                <div id="select_type_search" type_search="search_product" class="search_cat_btn">
                    <span class="material-icons">widgets</span>
                    <span class="hidden-xs hidden-sm">Productos</span>
                </div>
                <input class="search-input s-card-actv-input" autocomplete="off" type="text" id="searchInput"
                    name="searchInput" placeholder="Buscar.." />

                <button type="button" id="search_button_dell" class="search-delete  search-close"><span
                        class="material-icons">cancel</span>
                </button>
            </div>


            <div class="search-nav-icons">
                <button type="button" class="cart_button search-close right_nav_open">
                    <span class="material-icons"> store</span>
                    <span id="cart-number-1" class="cart_n cart-number">0</span>
                </button>
            </div>

        </div>
        <!--search-header -->
    </div>
    <!--search-body -->
    <div id="search-body" class="search-body ">
        <!--search-title -->
        <!--search-result -->
        <div class="search-title col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="btn-group dopdown">
                <button id="" m_id="{{{m_id}}}" m_t_id="{{{m_t_id}}}" m_t_name="{{{m_t_name}}}" type="button"
                    class="dropdown-toggle line  select-btn btn xl btn-more  dropdown-toggle btn-primary text"
                    data-toggle="dropdown" aria-expanded="true">
                    {{#each ws_left_nav_data.m_t}}
                    {{#ifEquals m_t_url ../board_type_name }}
                    <span class="material-icons">{{m_t_icon}}</span>
                    <span class="this_m_t_name">{{m_t_name}}</span>
                    {{/ifEquals}}
                    {{/each}}
                    <i class="material-icons">expand_more</i>
                </button>
                <ul class="dropdown-menu pull-left">
                    {{#each ws_left_nav_data.m_t}}
                    <li class="margin-5">
                        <a class="" onclick="get_module_type_nav(this)" m_id="{{{m_id}}}" m_t_id="{{{m_t_id}}}"
                            s_url_t_m="board" m_t_url="{{{m_t_url}}}" href="#">
                            <span class="material-icons">{{m_t_icon}}</span>
                            <span class="this_m_t_name">{{m_t_name}}</span>
                        </a>
                    </li>
                    {{/each}}
                </ul>
            </div>
             <span class=" btn-primary text material-icons">arrow_forward_ios</span>
            <button type="button" search_cat="search_product"
                class="search_button_cat btn xl btn-more  btn-primary line active">
                <i class="material-icons">widgets</i>
                <span search_m_t_name="product" class="hidden-xs hidden-sm">{{#with ws_lang_data}}
                    {{b_product}}{{/with}}</span>
            </button>
            <button type="button" search_cat="search_service"
                class="search_button_cat btn xl btn-more  btn-primary line">
                <i class="material-icons">home_repair_service</i>
                <span search_m_t_name="service" class="hidden-xs hidden-sm">{{#with ws_lang_data}}
                    {{b_service}}{{/with}}</span>
            </button>
            <button type="button" search_cat="search_contact"
                class="search_button_cat btn xl btn-more  btn-primary line">
                <i class="material-icons">people_alt</i>
                <span search_m_t_name="contact" class="hidden-xs hidden-sm">{{#with ws_lang_data}}
                    {{b_contact}}{{/with}}</span>
            </button>


                    {{#each ws_left_nav_data.m_t}}
                    {{#ifEquals m_t_url ../board_type_name }}
                <button type="button" search_cat="search_order_{{board_type_name}}"
                                class="search_button_cat btn xl btn-more  btn-primary line">
                                <i class="material-icons">{{m_t_icon}}</i>
                                <span search_m_t_name="contact" class="hidden-xs hidden-sm">{{#with ws_lang_data}}{{m_board_new_item_order}}{{/with}}    {{m_t_name}}  </span>
                            </button>
                    {{/ifEquals}}
                    {{/each}}
            <div class="btn-group dopdown">
                {{#each m_now}}
                <button search_cat="search_board" id="" m_id="{{{m_id}}}" m_t_id="{{{m_t_id}}}" type="button"
                    class="search_button_cat search_button_board  dropdown-toggle  select-btn dropdown-toggle btn xl btn-more  btn-primary line"
                    data-toggle="dropdown" aria-expanded="true">
                    <span class="material-icons">{{m_t_icon}}</span>
                    <span class="hidden-xs hidden-sm this_m_t_name">{{m_t_name}}</span>
                    <i class="hidden-xs hidden-sm material-icons">expand_more</i>
                </button>
                <ul class="dropdown-menu pull-right">
                    {{#each ../m_t}}
                    <li class="margin-5">
                        <a m_id="{{{m_id}}}" m_t_id="{{{m_t_id}}}" s_url_t_m="{{{m_url}}}"
                            class="search_button_board_li btn-status btn {{{m_t_color}}} text" href="#" role="button"
                            data-toggle="tooltip" data-placement="top" data-original-title="{{p_description}}">
                            <span class="material-icons">{{m_t_icon}}</span>
                            <span class="">{{m_t_name}}</span>
                        </a>
                        <span class="search_m_t hidden">
                            <i class="material-icons">{{m_t_icon}}</i>
                            <span search_m_t_name="{{m_t_name}}"
                                class="hidden-xs hidden-sm search_m_t_name">{{m_t_name}}</span>
                        </span>
                    </li>
                    {{/each}}
                </ul>
                {{/each}}
            </div>
        </div>
        <div class="search-result col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div id="card_order_result_items"> </div>
            <div id="card_contact_result_items"> </div>
            <div id="card_product_result_items"> </div>
        </div>

        <!--search-body -->
    </div>
    <!--searchBox -->
</div>
<script>


    //Boton select animaciones
    /*  $("li.bg-color").click(function () {
          var bgColor = $(this).attr('bg-color');
          $("#bg-select-color").removeClass();
          $("#bg-select-color").addClass('btn ' + bgColor + ' line');
          $("#bg-select").val(bgColor);
          $("#bg-select-color").attr('bg-color', bgColor);
          // $("#bg-select-color").attr('bg-color').html(bgColor);
      })
          //Efecto eye paleta de colores
          .mouseout(function () {
              $("span", this).first().text("panorama_fish_eye");
              // $("p", this).last().text(++i);
          })
          .mouseover(function () {
              $("span", this).first().text("check_circle");
          });
  */




</script>